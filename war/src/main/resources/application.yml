spring:
    application:
        name: zod
    config:
        import: application-dev.yml
    main:
        web-application-type: servlet
    thymeleaf:
        cache: false
        check-template-location: false
        encoding: UTF-8
        mode: HTML
server:
    port: 18080
    servlet:
        context-path: /
